/**
 * @deprecated Re-export of Shweta Foods seed data — use imports from `db/shweta-foods-data` in new code.
 * Kept as default export for existing `@/db/sample-data` consumers (fallback products, admin).
 */
export { default } from "./shweta-foods-data";
